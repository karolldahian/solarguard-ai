[project]
name = "solarguard-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "pandas>=3.0.5",
    "plotly>=7.0.0",
    "streamlit>=1.63.0",
    "tensorflow>=2.21.0",
]

[project.scripts]
solarguard-ai = "solarguard_ai:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
